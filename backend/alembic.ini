# Alembic configuration for the Petone backend.
# The database URL is injected at runtime from app.config.settings
# (see alembic/env.py) — intentionally not set here.
[alembic]
script_location = alembic
prepend_sys_path = .
